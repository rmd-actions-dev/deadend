#include <iostream>

#include "TcpServer.hh"

class TcpConnection;
class ConnectionManager;

int main()
{
//  boost::shared_ptr<deadend::tcpServer::TcpServer<TcpConnection, ConnectionManager>> server;
//
 // try {
  //  server.reset(new deadend::tcpServer::TcpServer<TcpConnection, ConnectionManager>("localhost", "8008"));
   // server->run();
 // }
 // catch (std::exception& e) {
 //  std::cout << e.what() << std::endl;
 // }
  return 0;
}

